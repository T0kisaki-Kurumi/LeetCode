1.this指针指向的是被调用的成员函数所属的对象。
2.如果成员函数形参和成员属性命名相同，必须显式地使用this，否则编译器都认为是形参。
3.非静态成员函数隐式地含有一个this指针作为形参，而静态成员函数没有，因此静态成员函数不能调用非静态的成员属性或成员函数。




链式编程：
Person & addAge(int age){
    this->age += age;
    return *this;
}
注意一定要返回一个引用，否则相当于新声明了当前对象的拷贝