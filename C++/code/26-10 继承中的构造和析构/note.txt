1. 类的继承中，先调用父类的构造函数，再调用子类的构造函数，析构的顺序相反
2. 可以利用初始化列表语法，显式地指定使用父类的某个构造函数。如果未指定，调用父类的默认构造函数
3. 子类不会继承父类中系统默认提供的函数，包括构造函数、析构函数、拷贝构造函数、赋值重载函数等。因为只有父类自己才知道如何构造和析构等。




初始化列表语法：
// 冒号后为基类
class Son2: public Base2{
public:
    // 冒号后为初始化列表
    Son2(): Base2(1){}
};