1.STL容器使用时机：
    ① vector：需要经常查看，但不删除的数据
    ② deque：对排队数据的存储，需要头尾的插入和删除
        * 随机访问和空间的大量释放，vector效率高于deque，因为vector是一段连续空间，而deque是多段
    ③ list：需要频繁插入和删除不确定位置的元素
    ④ set：需要自动地进行排序
    ⑤ map：需要快速通过键查找到值