1. 重载：同一个作用域下的同名函数。
    ① 同一个作用域
    ② 参数个数、参数顺序、参数类型不同
    ③ 和函数返回值没有关系
    ④ const的有无也可以作为重载条件

2. 重定义（隐藏）：
    ① 有继承
    ② 子类重新定义父类的同名成员（非virtual函数）
    ③ 会把父类中同名的所有重载版本都隐藏掉

3. 重写（覆盖）：
    ① 有继承
    ② 子类重写父类的虚函数
    ③ 函数返回值、名字、参数必须和父类中的虚函数一致