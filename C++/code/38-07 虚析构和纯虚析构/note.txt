1. 在使用多态时，默认情况下不会调用子类的析构函数，如果此时子类的析构函数有对堆区内容的释放则会导致释放不干净，内存泄漏。
   此时需要将父类的析构函数也声明为虚函数。
2. 如果在虚析构函数后面加 =0 让它变成纯虚析构函数，则一定要在类外实现，否则不会调用导致报错（因为析构是从子类到父类逐层调用）。
3. 虚函数在类外实现时不加virtual
4. 即使纯虚函数在类外实现了，所在的类还是抽象类，仍然不能实例化对象。