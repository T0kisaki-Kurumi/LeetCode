1.空类的大小是1
2.空类也可以实例化对象，每个对象在内存中都有独一无二的地址。
3.只有成员属性算在类的大小中，成员函数不算，静态成员属性不算，静态成员函数不算。（也就是说所有对象共享的东西都不算入类的大小）
4.静态成员只能在类内声明，在类外实现。（不实现的话调用时候会报错）



code:
1. # pragma pack(n) 设置编译器的内存对齐方式，n表示比特数，n可以取1，2，4，8，16