1. list和vector的区别：list每次插入和删除一个元素都会配置或者申请一个元素的空间，对于
   空间的运用绝对精准，没有浪费。
2. list对于任何位置的插入和删除都只需要常数时间
3. 系统提供的List容器是一个双向链表，因此空间和时间小号较大（每个位置都有一个prev和next记录前后节点的地址）
4. erase(begin, end) 删除[begin,end)区间的数据，返回下一个数据的位置（迭代器类型）
5. 由于list的迭代器不支持随机访问，不能使用algorithm库提供的标准算法。
   但是这类容器内部一般会提供相应算法的接口，例如sort