1. 类模板中使用友元函数的方法：
    ① 友元函数在类内实现，很简单（注意虽然在类内实现实际上还是一个全局函数）
    ② 友元函数在类外实现：
        1）类内要将其声明为一个模板函数（函数名后加<>）
        2）类外要将其声明在类模板之前让类里面知道有这么一个函数模板，实现可以马上写也可以之后写
           同时，在函数模板之前还要再声明类模板，让函数模板知道有这么一个类模板
2. 类模板和友元函数的深度分析：https://blog.csdn.net/dreamer_lhs/article/details/53580088